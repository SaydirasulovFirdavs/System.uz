## Packages
recharts | Dashboard analytics charts and data visualization
date-fns | Human-readable date formatting
framer-motion | Page transitions and sleek animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

The application uses Replit Auth. Ensure the user is logged in before accessing private routes.
The design implements a Premium Dark Mode with Glassmorphism and Neon highlights.
